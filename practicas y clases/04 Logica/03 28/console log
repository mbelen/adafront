inspect de Chrome:
x="19";
y=19;
console.log(x)


if(x==y){ console.log("son iguales");} 
devuelve: Son iguales
/* me dice que son iguales porque no es un lenguaje tipado (¿pero cómo entiende que "diecinueve" es 19???) */

if(x===y){ console.log("son equivalentes");}else{console.log("No son equivalentes")} 
devuelve: 
No son equivalentes

/* las sentencias SIEMPRE se separan con;
pero lo último que va antes de cerrar la llave, puede prescindir del ; siempre y cuando no sea una sentencia */

typeof(x)
devuelve: "string"

typeof(y)
devuelve: "number"

